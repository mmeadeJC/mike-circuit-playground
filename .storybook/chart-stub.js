/**
 * Stub for chart.js/auto - PrimeVue Chart requires it but we don't use charts in Storybook.
 * Prevents build failure when chart.js isn't installed.
 */
export default {};
export const Chart = () => null;
